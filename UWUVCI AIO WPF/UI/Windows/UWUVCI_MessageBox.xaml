<Window x:Class="UWUVCI_AIO_WPF.UI.Windows.UWUVCI_MessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UWUVCI Message"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        Background="#FFF3F3F3"
        SizeToContent="WidthAndHeight"
        BorderBrush="#FF2196F3"
        BorderThickness="1.5"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Background" Value="#FF2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF42A5F5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF1E88E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FF757575"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="6" Background="White" Padding="20" Margin="8">
        <StackPanel>
            <TextBlock x:Name="TitleBlock"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="#1A1A1A"
                       Margin="0,0,0,10"/>

            <TextBlock x:Name="MessageBlock"
                       TextWrapping="Wrap"
                       FontSize="13"
                       Foreground="#333"
                       Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        x:Name="ButtonPanel">
                <Button x:Name="BtnYes" Content="Yes" Width="80" Height="28"
                        Margin="0,0,8,0"
                        Click="BtnYes_Click" Visibility="Collapsed"/>
                <Button x:Name="BtnNo" Content="No" Width="80" Height="28"
                        Margin="0,0,8,0"
                        Click="BtnNo_Click" Visibility="Collapsed"/>
                <Button x:Name="BtnOk" Content="OK" Width="80" Height="28"
                        Margin="0,0,8,0"
                        Click="BtnOk_Click" Visibility="Collapsed"/>
                <Button x:Name="BtnCancel" Content="Cancel" Width="90" Height="28"
                        Click="BtnCancel_Click" Style="{StaticResource SecondaryButton}" Visibility="Collapsed"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
